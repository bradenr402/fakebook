<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <strong><%= post.user.name %></strong> posted: "<%= link_to post.body, post %>"
    </li>
    <%= render partial: 'likes/button', locals: { likeable: post } %>
  <% end %>
</ul>

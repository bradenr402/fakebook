<h1 class="font-bold m-6 text-4xl text-blue-500">FakeBook</h1>

<div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-full">
  <h1 class="font-bold text-center mb-4 text-3xl text-blue-500">Log In</h1>

  <div class="bg-white border border-zinc-800 border-opacity-10 p-8 shadow-xl rounded-lg ml-auto mr-auto w-2/5 max-w-96 min-w-80">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <div class="field">
        <%= f.label :email, 'Email address', class: 'sr-only' %>
        <%= f.email_field :email, autocomplete: "email", autofocus: true, placeholder: 'Email address', class: 'default-transition block px-2.5 pb-2.5 pt-4 w-full mb-4 text-sm bg-transparent rounded-md border border-zinc-800 border-opacity-15 focus:ring-1 focus:ring-blue-500' %>
      </div>

      <div class="field">
        <%= f.label :password, class: 'sr-only' %>
        <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Password', class: 'default-transition block px-2.5 pb-2.5 pt-4 w-full mb-4 text-sm bg-transparent rounded-md border border-zinc-800 border-opacity-15 focus:ring-1 focus:border-blue-500' %>

      </div>
      <% if devise_mapping.rememberable? %>
        <div class="field flex items-center mb-4">
          <%= f.check_box :remember_me, class: 'default-transition w-4 h-4 text-blue-500 bg-gray-100 border border-zinc-800 border-opacity-10 rounded focus:ring-blue-500 focus:ring-2' %>
          <%= f.label :remember_me , class: 'ms-2 text-sm text-gray-500' %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "Log In", class: 'btn-primary bg-blue-500 text-gray-50 font-medium rounded-md w-full px-2.5 py-2.5 mb-4 focus:ring-blue-500 focus:ring-2 outline-none border-2 border-transparent focus:border-white' %>
      </div>

    <% end %>
    <%= render "devise/shared/links" %>

  </div>
</div>

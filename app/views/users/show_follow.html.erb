<%= render @user %>

<h2 class="ml-5 mt-8 mb-4 font-medium"><%= @title %></h2>

<% if @users.any? %>
    <div class="all-users">
      <% @users.each do |user| %>
        <%= render 'user_link', user: user %>
      <% end %>
    </div>
<% end %>
